Configuration

This code was designed with the dijet+ISR analysis in mind, which has multiple spectra with different mass ranges. 
The dictionaries in scripts/configure_points.py specify these. For simpler cases of only 1 spectrum, follow the
same logic and have just one item in "spectra".

